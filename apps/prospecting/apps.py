from django.apps import AppConfig


class ProspectingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.prospecting"
    verbose_name = "Prospecção WhatsApp"
